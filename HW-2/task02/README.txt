Command:
python3 script.py | ./mathrev

We decompile the file in Ghidra. The code runs a for loop and expects input in some specific format, logic of which is given in the script. We need to run the loop until we find a number from the variables defined is greater than 1000. If the Tts and the flag is read. The loop starts by accessing the elements of an array and since there is no bound on the pointer, it can access the values of other local variables in the memory.

The script consists of the string which is given as input to the function.
